{% extends 'base.html.twig' %}

{% block stylesheets %}
    {% include 'renav.html.twig' %}
{% endblock %}


{% block title %}Edit Voyage{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-5 text-center">Modifier votre voyage</h1>

    {{ include('voyage/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex justify-content-around">
    	<a class="btn btn-primary" href="{{ path('voyage_index') }}">Retourner à la liste des voyages</a>
    </div>

    <div class="d-flex justify-content-around">
    	{{ include('voyage/_delete_form.html.twig') }}
	</div>
</div>
{% endblock %}
